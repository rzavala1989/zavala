# Booking link rendered in the Hero "Book a Chat" button and the Footer.
# In Vercel: Project Settings, Environment Variables. Apply to Production and Preview.
# Required at build time. Builds will fail with a clear error if this is unset.
PUBLIC_CALENDLY_URL=https://calendly.com/your-handle/15min
