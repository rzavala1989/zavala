<div class="masthead">
  <span class="masthead-mark">RZ</span>
  <span class="masthead-rule" aria-hidden="true"></span>
  <span class="masthead-meta">VOL. IX · HENDERSON, NV · MMXXVI</span>
  <span class="masthead-rule" aria-hidden="true"></span>
  <span class="masthead-meta">PORTFOLIO, No. 01</span>
</div>

<section class="hero" id="hero">
  <div class="hero-content">
    <div class="hero-text">
      <p class="hero-tagline">
        <span class="hero-tagline-num">01</span>
        <span class="hero-tagline-rule" aria-hidden="true"></span>
        The Engineer
      </p>

      <h1 class="hero-name">Ricardo<br/>Zavala</h1>
      <p class="hero-title">Staff Software Engineer</p>

      <p class="hero-statement">
        I build systems that stay correct when traffic spikes, data gets messy,
        and architecture starts fighting back.
      </p>

      <p class="hero-bio">
        Ten years shipping distributed systems, data-heavy products, and complex interfaces
        across federal, fintech, healthcare, and commerce.
      </p>

      <div class="hero-actions">
        <a class="hero-primary" href="#projects">Selected Work <span aria-hidden="true">→</span></a>
        <a class="hero-secondary" href="/resume/">Resume</a>
      </div>

      <div class="hero-meta-links" aria-label="Additional links">
        <a class="link" href="https://github.com/rzavala1989" target="_blank" rel="noopener noreferrer">GitHub</a>
        <a class="link" href="/writing/">Engineering Notes</a>
      </div>
    </div>

    <div class="hero-image">
      <figure class="hero-figure">
        <img
          src="/images/headshot-hero.png"
          alt="Portrait of Ricardo Zavala, Staff Software Engineer"
          width="600"
          height="750"
          fetchpriority="high"
          decoding="async"
        />
        <figcaption class="hero-plate" aria-hidden="true">
          <span class="visually-hidden">Section 1</span>PLATE I
        </figcaption>
      </figure>
    </div>
  </div>
</section>

<style>
  .masthead {
    display: flex;
    align-items: center;
    gap: 1rem;
    max-width: var(--content-max-width);
    margin: 0 auto;
    padding: 1.5rem 1.5rem 0;
    font-family: var(--font-body);
    font-size: var(--fs-eyebrow);
    font-weight: 500;
    letter-spacing: var(--tracking-widest);
    text-transform: uppercase;
    color: var(--color-muted);
  }

  .masthead-mark {
    font-family: var(--font-heading);
    font-style: italic;
    font-size: 1.1rem;
    letter-spacing: 0;
    text-transform: none;
    color: var(--color-text);
  }

  .masthead-rule {
    flex: 1;
    height: 1px;
    background: var(--color-divider);
  }

  .masthead-meta {
    white-space: nowrap;
  }

  .hero {
    min-height: clamp(620px, 78vh, 760px);
    display: flex;
    align-items: center;
    padding: 2rem 1.5rem 3rem;
  }

  .hero-content {
    max-width: var(--content-max-width);
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 4rem;
    align-items: center;
    width: 100%;
  }

  @media (prefers-reduced-motion: no-preference) {
    .hero-content {
      opacity: 0;
      animation: fadeIn 0.8s var(--ease-out) 0.1s forwards;
    }
  }

  .hero-text {
    max-width: 560px;
  }

  .hero-tagline {
    display: flex;
    align-items: center;
    font-family: var(--font-body);
    font-weight: 500;
    font-size: var(--fs-eyebrow);
    letter-spacing: var(--tracking-widest);
    text-transform: uppercase;
    color: var(--color-muted);
    margin-bottom: 1rem;
  }

  .hero-tagline-num {
    font-family: var(--font-heading);
    font-style: italic;
    font-size: 1em;
    color: var(--bronze);
    margin-right: 0.6rem;
  }

  .hero-tagline-rule {
    display: inline-block;
    width: 2.5rem;
    height: 1px;
    background: var(--color-divider);
    vertical-align: middle;
    margin-right: 0.8rem;
  }

  .hero-name {
    font-family: var(--font-heading);
    font-weight: 400;
    font-size: clamp(3.4rem, 7vw, 5.4rem);
    line-height: 0.96;
    letter-spacing: var(--tracking-display);
  }

  .hero-title {
    font-family: var(--font-body);
    font-weight: 500;
    font-size: clamp(0.82rem, 1.4vw, 1rem);
    letter-spacing: var(--tracking-widest);
    text-transform: uppercase;
    color: var(--color-secondary);
    margin-top: 1.25rem;
  }

  .hero-statement {
    font-family: var(--font-heading);
    font-size: clamp(1.35rem, 2.2vw, 1.85rem);
    line-height: 1.2;
    letter-spacing: var(--tracking-tight);
    color: var(--color-text);
    margin-top: 1.5rem;
    max-width: 33rem;
  }

  .hero-bio {
    font-size: var(--fs-body);
    line-height: 1.65;
    color: var(--color-secondary);
    margin-top: 1rem;
    max-width: 31rem;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.85rem;
    margin-top: 1.65rem;
  }

  .hero-primary,
  .hero-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 42px;
    padding: 0.66rem 0.95rem;
    font-size: var(--fs-small);
    font-weight: 500;
    letter-spacing: 0.03em;
    text-decoration: none;
    transition: background-color var(--dur-med) ease, color var(--dur-med) ease, border-color var(--dur-med) ease;
  }

  .hero-primary {
    background: var(--ink);
    border: 1px solid var(--ink);
    color: var(--paper);
  }

  .hero-primary:hover,
  .hero-primary:focus-visible {
    background: var(--bronze);
    border-color: var(--bronze);
  }

  .hero-secondary {
    border: var(--border-hair);
    color: var(--ink);
  }

  .hero-secondary:hover,
  .hero-secondary:focus-visible {
    border-color: var(--ink);
  }

  .hero-meta-links {
    display: flex;
    flex-wrap: wrap;
    gap: 1.25rem;
    margin-top: 0.9rem;
  }

  .hero-figure {
    display: inline-flex;
    flex-direction: column;
  }

  .hero-plate {
    font-family: var(--font-body);
    font-size: var(--fs-eyebrow);
    font-weight: 500;
    letter-spacing: var(--tracking-widest);
    color: var(--color-muted);
    margin-top: 0.45rem;
  }

  .hero-image {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-image img {
    max-width: 100%;
    height: auto;
    max-height: 500px;
    object-fit: contain;
    border: var(--border-hair);
    background: var(--paper-alt);
  }

  @media (max-width: 768px) {
    .hero {
      min-height: auto;
    }

    .hero-content {
      grid-template-columns: 1fr;
      gap: 2rem;
      text-align: center;
    }

    .hero-text {
      order: 2;
      max-width: 100%;
    }

    .hero-image {
      order: 1;
    }

    .hero-image img {
      max-height: 320px;
    }

    .hero-tagline,
    .hero-actions,
    .hero-meta-links {
      justify-content: center;
    }

    .hero-statement,
    .hero-bio {
      margin-left: auto;
      margin-right: auto;
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding: 1.5rem 1rem 2.5rem;
    }

    .hero-name {
      font-size: clamp(3.2rem, 18vw, 4.4rem);
    }

    .hero-image img {
      max-height: 250px;
    }

    .masthead-meta {
      font-size: 0.55rem;
      letter-spacing: 0.08em;
    }

    .hero-actions {
      align-items: stretch;
    }

    .hero-primary,
    .hero-secondary {
      flex: 1 1 9rem;
    }
  }
</style>
